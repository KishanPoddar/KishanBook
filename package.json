{
  "name": "kishanbook-backend",
  "version": "1.0.0",
  "description": "KishanBoook - Your notebook on the cloud",
  "main": "index.js",
  "scripts": {
    "start": "nodemon ./index.js",
    "build": "npm install --production=false && cd client && npm install && npm run build ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.14.3",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
